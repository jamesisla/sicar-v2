# ─── Base de datos ────────────────────────────────────────────
# Tipo: postgres | oracle | mysql | sqlite
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=sicar
DB_PASSWORD=sicar123
DB_NAME=sicar_v2
DB_SYNC=false        # true solo en desarrollo inicial
DB_LOGGING=false
DB_SSL=false

# Para Oracle (producción):
# DB_TYPE=oracle
# DB_HOST=oracle-host
# DB_PORT=1521
# DB_NAME=SICAR       # SID o Service Name
# DB_USER=mbnowner
# DB_PASSWORD=

# ─── JWT ──────────────────────────────────────────────────────
JWT_SECRET=cambia-esto-en-produccion
JWT_REFRESH_SECRET=cambia-esto-refresh-en-produccion

# ─── SIGFE (integración contable) ─────────────────────────────
SIGFE_URL=
SIGFE_USUARIO=
SIGFE_CODIGO_INSTITUCION=
SIGFE_CORREO_NOTIFICACION=
SIGFE_PARTIDA=
SIGFE_CAPITULO=
SIGFE_AREA_TRANSACCIONAL=

# ─── TGR (pagos en línea) ─────────────────────────────────────
TGR_URL=
TGR_API_KEY=

# ─── App ──────────────────────────────────────────────────────
PORT=3001
CORS_ORIGINS=http://localhost:3000
NODE_ENV=development
